import {styles as stylesLayout} from '@common-styles/layout';
import {screensStyles} from '@common-styles/screens';

export const {col, row, centerXY} = stylesLayout;
export const {item} = screensStyles;
export const mt25 = {marginTop: 25};
